"""Response-side firewall — canary tokens + output filtering.

Where the detectors in src/detectors/ guard the *request* (what comes IN),
the modules here guard the *reply* (what goes OUT to the user)."""
